import Loading from "@/components/loading";

const FeedModalLoading = () => {
    return (
        <div className="modal">
            <Loading />
        </div>
    );
};

export default FeedModalLoading;
